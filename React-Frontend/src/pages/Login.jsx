import { motion } from 'motion/react'

import './css/Common.css'

export default function Login({ toastVersion, setToastVersion, toastContent, setToastContent }) {
    return (
        <motion.div>Todo: Login</motion.div>
    )
}
